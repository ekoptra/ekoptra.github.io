import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme) => ({
  chartCardWrapper: {
    height: "400px",
  },

  chartCard: {
    height: "1000px",
  },
}));

export default useStyles;
